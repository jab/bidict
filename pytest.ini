[pytest]
testpaths = bidict tests docs
doctest_optionflags = ELLIPSIS
addopts =
  -vv
  --numprocesses=auto
  --benchmark-disable
  --doctest-modules
  --doctest-glob=tests/*.txt
  --doctest-glob=docs/*.rst
